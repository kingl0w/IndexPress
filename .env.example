# Typesense admin client (used by scripts/index-typesense.ts)
TYPESENSE_HOST=localhost
TYPESENSE_PORT=8108
TYPESENSE_API_KEY=your-admin-api-key-here

# Typesense search client (exposed to browser — use a SEARCH-ONLY key)
NEXT_PUBLIC_TYPESENSE_HOST=localhost
NEXT_PUBLIC_TYPESENSE_PORT=8108
NEXT_PUBLIC_TYPESENSE_SEARCH_API_KEY=your-search-only-key-here
NEXT_PUBLIC_TYPESENSE_PROTOCOL=http
